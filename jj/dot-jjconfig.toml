[ui]
default-command = ["log", "-r", "@ | ancestors(remote_bookmarks().., 2) | trunk()"]
diff-editor = ["nvim", "-c", "DiffEditor $left $right $output"]
diff-instructions = false

[templates]
git_push_bookmark = '"sgilson/push-" ++ change_id.short()'

